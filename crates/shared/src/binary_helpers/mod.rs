/// Exposes the `BinaryError` enum with its variants
pub mod bin_error;

/// Exposes helper functions for conversions between primitive data types
pub mod conversions;

/// Exposes helpers for working with raw binary data stored in little-endian format
pub mod le;
